name: Deploy Website App

on:
  push:
    branches:
      - dev
      - qa

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      build-path: ${{ steps.build.outputs.build-path }}/app-test/dist/browser

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: ./app-test
        run: npm install

      - name: Build Angular app
        id: build
        working-directory: ./app-test
        run: |
          npm run build --prod
      
      - name: List build output
        working-directory: ./app-test
        run: ls -R ./dist
  configure-aws:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Checkout code
        uses: actions/checkout@v2
      - name: Terraform Init
        working-directory: ./terraform/environments/${{ github.ref_name }}
        run: |
          terraform init

      - name: Terraform Plan
        working-directory: ./terraform/environments/${{ github.ref_name }}
        run: |
          terraform workspace select ${{ github.ref_name }} || terraform workspace new ${{ github.ref_name }}
          terraform plan

      - name: Terraform Apply
        working-directory: ./terraform/environments/${{ github.ref_name }}
        run: |
          terraform workspace select ${{ github.ref_name }} || terraform workspace new ${{ github.ref_name }}
          terraform apply -auto-approve

      - name: Sync S3 bucket
        run: |
          aws s3 sync ${{ needs.build.outputs.build-path }} s3://angular-website-terraform2-${{ github.ref_name }}/ --delete
